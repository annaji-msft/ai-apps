# 03 - Ports (CLI)

```bash
./run.sh
./run.ps1
```

## What this shows

| Command | What it does |
|---|---|
| `aca sandbox port add --port 8080 --anonymous` | Get a public URL |
| `aca sandbox port remove --port 8080` | Take it back down |
| `aca sandbox port list` | List exposed ports |
