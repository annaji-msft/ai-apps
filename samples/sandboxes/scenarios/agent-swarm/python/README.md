# Agent swarm (Python)

```bash
pip install -r requirements.txt
python swarm.py
```

Defaults to `N=3` mappers. Override:

```bash
NUM_MAPPERS=5 python swarm.py
```

Prints the top-10 most frequent words after the mapper + reducer pass.
