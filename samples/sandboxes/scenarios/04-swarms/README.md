# 04-swarms - Sandbox swarms

> **Coming soon.** This scenario is a placeholder. Track progress in [samples/sandboxes/README.md](../../README.md).

Many sandboxes, one orchestrator. Fan-out the same task across N workers (parallel evals, batch inference, distributed builds). Composes guides 01 (parallel section) + 11 (labels) + 04 (volumes).

